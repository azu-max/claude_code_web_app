name = "snapshare-url-shortener"
main = "src/index.ts"
compatibility_date = "2024-11-21"

# KVネームスペース（URL短縮のデータストア）
# 実際のデプロイ時は wrangler kv:namespace create で作成したIDを設定
# [[kv_namespaces]]
# binding = "URL_SHORTENER"
# id = "YOUR_KV_NAMESPACE_ID"

# 開発環境用の設定
[env.dev]
# 開発時はメモリ内KVを使用
